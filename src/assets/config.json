[
  {
    "configProp": "debugInfo",
    "widget": "radio",
    "defaultValue": false,
    "description": "Do you wanna send debug info?",
    "options": [
      {
        "description": "Yeah!",
        "value": true
      },
      {
        "description": "There's no way in heaven that this could happen",
        "value": false
      }
    ]
  },
  {
    "configProp": "detailLevel",
    "widget": "slider",
    "defaultValue": 20,
    "description": "Choose the detail level",
    "options": {
      "min": 0,
      "max": 100,
      "step": 10
    }
  },
  {
    "configProp": "maskingRules",
    "widget": "list",
    "defaultValue": [
      "~/",
      "~/apps",
      "~/Documents"
    ],
    "description": "Please specify where should we look",
    "options": {
      "placeholder": "Path",
      "defaultValue": ""
    }
  }
]
